type:
  # Filter out the following item types
  excluded:
    - Material
  # Keep following item types, ignoring whether other configurations exclude these
  included:
    - Rarity
name:
  # Filter out the following item names
  excluded:
    - Gel Drop
  # Keep following item names, ignoring whether other configurations exclude these
  included:
    - Ecto Drop